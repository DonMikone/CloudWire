# Ad-hoc signed distribution until a Developer ID exists

Releases are ad-hoc signed DMGs; users approve the first launch in System Settings, and macOS asks once per update for Keychain access. The release workflow already signs and notarises when Apple secrets are configured. No Homebrew cask, because Homebrew requires notarisation.
